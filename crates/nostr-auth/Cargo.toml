[package]
name = "gittree-nostr-auth"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
gittree-core = { path = "../core" }
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
secp256k1 = "0.29"
sha2 = "0.10"

[dev-dependencies]
