[package]
name = "gittree-relay-adapter"
version = "0.1.0"
edition = "2024"

[lib]
path = "src/lib.rs"

[dependencies]
async-trait = "0.1"
gittree-core = { path = "../core" }

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }
