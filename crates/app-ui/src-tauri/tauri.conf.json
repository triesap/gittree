{
  "$schema": "../node_modules/@tauri-apps/cli/config.schema.json",
  "identifier": "ee.gittr.app",
  "productName": "gittree",
  "mainBinaryName": "gittree",
  "build": {
    "frontendDist": "../dist",
    "devUrl": "http://localhost:3340",
    "beforeDevCommand": "bash -lc 'root=\"$(git rev-parse --show-toplevel)\"; cd \"$root/crates/app-ui\" && npm install && npm run tw:watch & trunk serve --dist dist --port 3340'",
    "beforeBuildCommand": "bash -lc 'root=\"$(git rev-parse --show-toplevel)\"; cd \"$root/crates/app-ui\" && npm install && npm run tw:build && trunk build --dist dist'"
  },
  "app": {
    "security": {
      "assetProtocol": {
        "enable": true
      }
    },
    "windows": [
      {
        "label": "main",
        "title": "gittree",
        "resizable": true,
        "fullscreen": false,
        "visible": true,
        "width": 1240,
        "height": 775
      }
    ]
  },
  "bundle": {
    "active": false,
    "targets": "all"
  }
}
